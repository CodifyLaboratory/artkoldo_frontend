import React from "react";

export default function MainPage() {
  const [filters, setFilters] = useState({
    categoryId: 1,
  });

  return <Container fluid className="catalog-container"></Container>;
}
